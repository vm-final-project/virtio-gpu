diff --git a/lib/posix-sysinfo/sysinfo.c b/lib/posix-sysinfo/sysinfo.c
index f131487..3d0bb0e 100644
--- a/lib/posix-sysinfo/sysinfo.c
+++ b/lib/posix-sysinfo/sysinfo.c
@@ -39,6 +39,7 @@
 #include <uk/essentials.h>
 #include <uk/config.h>
 #include <sys/sysinfo.h>
+#include <uk/pcpuvar.h>
 #include <uk/syscall.h>
 
 #ifdef CONFIG_LIBUKPAGING
@@ -216,7 +217,7 @@ UK_SYSCALL_R_DEFINE(int, getcpu, unsigned int *, cpu, unsigned int *, node,
 	/* tcache is ignored since Linux 2.6.24 */
 
 	if (cpu)
-		*cpu = 0;
+		*cpu = (unsigned int)uk_pcpuvar_current_get(uk_pcpuvar_cpu_idx);
 
 	if (node)
 		*node = 0;
diff --git a/lib/ukallocbbuddy/bbuddy.c b/lib/ukallocbbuddy/bbuddy.c
index 3e39480..84b760f 100644
--- a/lib/ukallocbbuddy/bbuddy.c
+++ b/lib/ukallocbbuddy/bbuddy.c
@@ -49,6 +49,7 @@
 #include <uk/alloc_impl.h>
 #include <uk/arch/limits.h>
 #include <uk/atomic.h>
+#include <uk/spinlock.h>
 #include <uk/bitops/bitscan.h>
 #include <uk/print.h>
 #include <uk/assert.h>
@@ -82,8 +83,42 @@ struct uk_bbpalloc {
 	unsigned long nr_free_pages;
 	struct uk_hlist_head free_head[FREELIST_SIZE];
 	struct uk_bbpalloc_memr *memr_head;
+	/* SMP: the free lists above are global mutable state. Unikraft's
+	 * allocator historically assumed a single processing LCPU; with the
+	 * A3 per-LCPU schedulers, secondary vCPUs allocate/free concurrently,
+	 * so all palloc/pfree must be serialised (cf. Linux: data shared
+	 * between cores must be spinlock-protected).
+	 */
+	__spinlock lock;
 };
 
+/* SMP locking wrappers: the real work is in *_locked(); these serialise it. */
+static void *bbuddy_palloc_locked(struct uk_alloc *a, unsigned long num_pages);
+static void bbuddy_pfree_locked(struct uk_alloc *a, void *obj,
+				unsigned long num_pages);
+
+static void *bbuddy_palloc(struct uk_alloc *a, unsigned long num_pages)
+{
+	struct uk_bbpalloc *const b = (struct uk_bbpalloc *)&a->priv;
+	unsigned long flags;
+	void *res;
+
+	uk_spin_lock_irqsave(&b->lock, flags);
+	res = bbuddy_palloc_locked(a, num_pages);
+	uk_spin_unlock_irqrestore(&b->lock, flags);
+	return res;
+}
+
+static void bbuddy_pfree(struct uk_alloc *a, void *obj, unsigned long num_pages)
+{
+	struct uk_bbpalloc *const b = (struct uk_bbpalloc *)&a->priv;
+	unsigned long flags;
+
+	uk_spin_lock_irqsave(&b->lock, flags);
+	bbuddy_pfree_locked(a, obj, num_pages);
+	uk_spin_unlock_irqrestore(&b->lock, flags);
+}
+
 #if CONFIG_LIBUKALLOCBBUDDY_FREELIST_SANITY
 /* Provide sanity checking of freelists, walking their length and checking
  * for consistency. Useful when suspecting memory corruption.
@@ -356,7 +391,7 @@ static inline void bbuddy_trim(struct uk_bbpalloc *b, struct chunk_head *ch,
 	}
 }
 
-static void *bbuddy_palloc(struct uk_alloc *a, unsigned long num_pages)
+static void *bbuddy_palloc_locked(struct uk_alloc *a, unsigned long num_pages)
 {
 	struct uk_bbpalloc *const b = (struct uk_bbpalloc *)&a->priv;
 	struct chunk_head *alloc_ch;
@@ -460,7 +495,8 @@ static inline size_t bbuddy_merge(struct uk_bbpalloc *b,
 	return ord;
 }
 
-static void bbuddy_pfree(struct uk_alloc *a, void *obj, unsigned long num_pages)
+static void bbuddy_pfree_locked(struct uk_alloc *a, void *obj,
+				unsigned long num_pages)
 {
 	struct uk_bbpalloc *const b = (struct uk_bbpalloc *)&a->priv;
 	char *base = obj;
@@ -665,6 +701,7 @@ struct uk_alloc *uk_allocbbuddy_init(void *base, size_t len)
 	for (i = 0; i < FREELIST_SIZE; i++)
 		UK_INIT_HLIST_HEAD(&b->free_head[i]);
 	b->memr_head = NULL;
+	uk_spin_init(&b->lock);
 
 	/* initialize and register allocator interface */
 	uk_alloc_init_palloc(a, bbuddy_palloc, bbuddy_pfree,
diff --git a/lib/ukboot/boot.c b/lib/ukboot/boot.c
index 0cd058c..0c1810d 100644
--- a/lib/ukboot/boot.c
+++ b/lib/ukboot/boot.c
@@ -362,6 +362,13 @@ void uk_boot_entry(void)
 	if (unlikely(!s))
 		UK_CRASH("Failed to initialize scheduling\n");
 	uk_sched_start(s);
+#if CONFIG_LIBUKSCHEDCOOP_SMP
+	/* Bring up one cooperative scheduler instance per secondary vCPU. The
+	 * allocators a/sa/auxsa are the same ones used by uk_schedcoop_create()
+	 * just above and are in scope here.
+	 */
+	uk_schedcoop_smp_bringup(a, sa, auxsa);
+#endif /* CONFIG_LIBUKSCHEDCOOP_SMP */
 #endif /* CONFIG_LIBUKBOOT_INITSCHED */
 
 	ictx.cmdline.argc = boot_argc;
diff --git a/lib/ukprint/Config.uk b/lib/ukprint/Config.uk
index bebcdd9..20aee6e 100644
--- a/lib/ukprint/Config.uk
+++ b/lib/ukprint/Config.uk
@@ -8,6 +8,7 @@ if LIBUKPRINT
 
 menuconfig LIBUKPRINT_PRINTK
 	bool "Kernel messages (uk_printk)"
+	select LIBUKLOCK
 	default y
 	help
 		Enables kernel message console.
diff --git a/lib/ukprint/console.c b/lib/ukprint/console.c
index 624bfe8..881f8d5 100644
--- a/lib/ukprint/console.c
+++ b/lib/ukprint/console.c
@@ -15,6 +15,7 @@
 #include <uk/print.h>
 #include <uk/errptr.h>
 #include <uk/lcpu.h>
+#include <uk/spinlock.h>
 #if CONFIG_LIBUKPRINT_PRINT_THREAD
 #include <uk/thread.h>
 #endif /* CONFIG_LIBUKPRINT_PRINT_THREAD */
@@ -27,6 +28,7 @@
 #include "print.h"
 
 unsigned int uk_print_console_lvl = UK_PRINT_KLVL_MAX;
+static struct uk_spinlock console_lock = UK_SPINLOCK_INITIALIZER();
 
 #if CONFIG_LIBUKPRINT_ANSI_COLOR
 #define LVLC_RESET	UK_ANSI_MOD_RESET
@@ -190,6 +192,7 @@ void uk_print_console_write(struct uk_print_msg *msg)
 {
 	char lbuf[BUFLEN];
 	int len, llen;
+	unsigned long flags;
 	const char *msghdr = __NULL;
 	const char *lptr = __NULL;
 	const char *nlptr = __NULL;
@@ -203,6 +206,11 @@ void uk_print_console_write(struct uk_print_msg *msg)
 		return;
 #endif /* CONFIG_LIBUKPRINT_PRINTK_UKSTORE */
 
+	/* A printk is assembled from multiple console writes. Serialize the
+	 * complete message so output from another LCPU cannot split a record.
+	 */
+	uk_spin_lock_irqsave(&console_lock, flags);
+
 	/*
 	 * Note: We reset the console colors earlier in order to exclude
 	 *       background colors for trailing white spaces.
@@ -307,4 +315,6 @@ void uk_print_console_write(struct uk_print_msg *msg)
 		len -= llen;
 		lptr = nlptr + 1;
 	}
+
+	uk_spin_unlock_irqrestore(&console_lock, flags);
 }
