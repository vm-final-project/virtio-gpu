// Generated from results/vogue_latest_evaluation_matrix.json; do not edit by hand.
#figure(
  text(size: 7pt, table(
    columns: (1.25in, 0.95in, 3.4in),
    inset: 2pt,
    align: (left, left, left),
    table.header([Row], [Status], [Evidence / allowed conclusion]),
    [#raw("disp.2d", block: false)], [#raw("pass", block: false)], [#raw("VirtIO-GPU 2D command ordering, DMA backing, scanout, flush, and fence synchronization work in the native harness.", block: false)],
    [#raw("proto.api-contract", block: false)], [#raw("pass", block: false)], [#raw("Guest API contract and fake-backend semantics are covered.", block: false)],
    [#raw("gfx.kmscube.sw", block: false)], [#raw("pass", block: false)], [#raw("Upstream kmscube source can be supported through VOGUE shims and software-rendered scanout; native software-path frame cost is measured.", block: false)],
    [#raw("gfx.glmark2.sw", block: false)], [#raw("pass", block: false)], [#raw("The scene-clear subset exercises EGL-style clear/present costs through the same substrate and reports native frame cost.", block: false)],
    [#raw("xport.gl-probe", block: false)], [#raw("pass", block: false)], [#raw("The tested host exposes a GL-capable VirtIO-GPU path for future K1 work.", block: false)],
    [#raw("gfx.kmscube.submit", block: false)], [#raw("blocked:missing-pass-marker", block: false)], [#raw("No submit claim; current K1 evidence is blocked:missing-pass-marker.", block: false)],
    [#raw("gfx.kmscube.frame", block: false)], [#raw("blocked:missing-pass-marker", block: false)], [#raw("Only a non-blank screendump whose mean colour matches the encoded CLEAR colour per frame may promote this row.", block: false)],
    [#raw("vk.drm-shim", block: false)], [#raw("pass", block: false)], [#raw("Mesa/Linux virtgpu DRM ioctl surface is translated to VirtIO-GPU protocol by the shim; native test passes against fake backend with Venus (id=4) ", block: false)],
    [#raw("proto.real-driver", block: false)], [#raw("pass", block: false)], [#raw("The project-local real backend implements the required 3D/blob/context/submit controlq surface and passes static/native gates.", block: false)],
    [#raw("xport.qemu-vgpu", block: false)], [#raw("blocked:probe-incomplete", block: false)], [#raw("If pass: real QEMU VirtIO-GPU device reached the guest driver. If blocked: only the named transport blocker is established.", block: false)],
    [#raw("vk.readiness", block: false)], [#raw("pass", block: false)], [#raw("Benchmark/evaluation artifacts are generated and preserve the acceleration claim boundary.", block: false)],
    [#raw("proto.venus-enc", block: false)], [#raw("pass", block: false)], [#raw("Venus encoder uses PACKED wire format (no inter-field alignment padding; uint64 not 8-byte aligned in stream); array presence fields are uint32 a", block: false)],
    [#raw("proto.venus-ring", block: false)], [#raw("pass", block: false)], [#raw("Mesa-compatible Venus ring-buffer protocol (vkCreateRingMESA / vkNotifyRingMESA) is implemented in libukvenus and passes all native tests: HEAD a", block: false)],
    [#raw("vk.smoke", block: false)], [#raw("pass", block: false)], [#raw("Host-side Vulkan API surface proof (alloc, map, fence) and Venus capset detection. vk.drm-shim (libukvirtgpu-drm) and vk.icd (Vulkan ICD) substra", block: false)],
    [#raw("gfx.vkmark", block: false)], [#raw("pass", block: false)], [#raw("vkmark Unikraft port uses libukvk-icd (vk.icd) over libukvirtgpu-drm (vk.drm-shim); ICD init and Venus context creation PASS; 10 scenes documente", block: false)],
    [#raw("host.baseline.vk", block: false)], [#raw("pass", block: false)], [#raw("Standards-track baseline: this validates QEMU + virglrenderer + Venus + Mesa Venus ICD + upstream ggml-vulkan on the same host hardware.", block: false)],
    [#raw("host.vk.probe", block: false)], [#raw("blocked:wrong-domain-artifact", block: false)], [#raw("No Unikraft Vulkan loader claim; current artifact status is blocked:wrong-domain-artifact.", block: false)],
    [#raw("bld.host.vk", block: false)], [#raw("pass", block: false)], [#raw("The full llama.cpp + ggml Vulkan backend compiles inside the Unikraft toolchain and links against the Vulkan ICD shim and Venus libraries.", block: false)],
    [#raw("host.bench.vk.run", block: false)], [#raw("blocked:wrong-domain-artifact", block: false)], [#raw("No Unikraft Vulkan runtime claim; current artifact status is blocked:wrong-domain-artifact.", block: false)],
    [#raw("host.bench.vk", block: false)], [#raw("blocked:wrong-domain-artifact", block: false)], [#raw("No Unikraft Vulkan throughput comparison claim; current artifact status is blocked:wrong-domain-artifact.", block: false)],
    [#raw("vk.ggml-dispatch", block: false)], [#raw("pass", block: false)], [#raw("164/164 checks pass in uk-ggml-vulkan-dispatch-test: proc lookup (80 functions), dispatch init via vk.drm-shim DRM fake backend, per-stub VK-SUCC", block: false)],
    [#raw("llm.bench.cpu", block: false)], [#raw("pass", block: false)], [#raw("Upstream llama.cpp CPU path boots on Unikraft with upstream sources unmodified. pp512=9.3 t/s via 9pfs model delivery.", block: false)],
    [#raw("llm.server.cpu", block: false)], [#raw("pass", block: false)], [#raw("Upstream llama.cpp server appliance boots directly into a single entrypoint on Unikraft (no shell).", block: false)],
    [#raw("llm.bench.vk", block: false)], [#raw("blocked:no-pass-line", block: false)], [#raw("Blocked; no throughput claim.", block: false)],
    [#raw("llm.server.vk", block: false)], [#raw("blocked:not-run", block: false)], [#raw("Blocked; no server-runtime claim. Vulkan runtime is gated on host EGL render-node availability.", block: false)],
    [#raw("bld.uk.vk", block: false)], [#raw("pass", block: false)], [#raw("Upstream llama.cpp Vulkan image builds with ggml-vulkan and Venus libraries linked. Build-pass confirms toolchain compatibility between Unikraft ", block: false)],
    [#raw("llm.bench.vk.real", block: false)], [#raw("blocked:egl-not-initialized", block: false)], [#raw("Blocked; no throughput claim.", block: false)],
  )),
  caption: [Current generated evidence matrix from `results/vogue_latest_evaluation_matrix.json`: 27 rows, 18 PASS, 9 blocked. Blocked rows are documented blockers, not failed measurements or PASS claims.]
) <tab:evidence>
