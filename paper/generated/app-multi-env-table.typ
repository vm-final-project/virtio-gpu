// Generated by scripts/app_multi_env_bench.py; do not edit by hand.
#figure(
  text(size: 7pt, table(
    columns: (0.8in, 1.6in, 1.3in, 1.6in),
    inset: 2pt,
    align: (left, left, left, left),
    table.header([*App*], [*Environment*], [*Status*], [*Evidence / Note*]),
    [`kmscube`], [Native fake backend], [`pass`], [gfx.kmscube.sw: 60 frames @ 469.3 FPS (640x480, BG],
    [`kmscube`], [QEMU + Unikraft CPU (2D path)], [`pass-substrate:cpu`], [gfx.kmscube.sw path is substrate evidence; xport.q],
    [`kmscube`], [QEMU + Unikraft VirtIO-GPU Vulkan], [`pass`], [K1: same-run QEMU frame proof passes.],
    [`glmark2`], [Native fake backend], [`pass`], [gfx.glmark2.sw: 120 frames @ 187.8 FPS (1280x800)],
    [`glmark2`], [QEMU + Unikraft CPU (2D path)], [`pass-substrate:cpu`], [gfx.glmark2.sw path: substrate FPS measured native],
    [`glmark2`], [QEMU + Unikraft VirtIO-GPU Vulkan], [`blocked:not-planned`], [No accelerated glmark2 scene gate is currently imp],
    [`vkmark`], [Native substrate (vk.drm-shim+vk.icd I], [`pass`], [gfx.vkmark: ICD init, Venus context, 10 scenes; FP],
    [`vkmark`], [Baremetal Vulkan GPU (NVIDIA)], [`pass`], [Host NVIDIA baseline: clear=85000 fps.],
    [`vkmark`], [Baremetal Vulkan CPU (llvmpipe)], [`pass`], [Host llvmpipe baseline: clear=3200 fps.],
    [`vkmark`], [QEMU + Unikraft VirtIO-GPU Vulkan], [`blocked:render-payload-not-implemented`], [QEMU vkmark scene rendering is not implemented yet],
    [`vulkan-smoke`], [Native substrate (Venus capset probe)], [`pass`], [vk.smoke: host Vulkan alloc/map/fence baseline + V],
    [`vulkan-smoke`], [QEMU + Unikraft VirtIO-GPU Vulkan], [`blocked:render-payload-not-implemented`], [QEMU smoke rendering is not implemented yet.],
    [`llama-upstream`], [QEMU + Unikraft CPU bench-only], [`pass`], [True llama.cpp CPU appliance: main() selects bench],
    [`llama-upstream`], [QEMU + Unikraft CPU server-only], [`pass`], [CPU server appliance reaches READY directly from m],
    [`llama-upstream-vk`], [Static ggml-vulkan/Venus dispatch], [`pass`], [Minimal local support layer for upstream ggml-vulk],
    [`llama-upstream-vk`], [QEMU + Unikraft Vulkan bench-only], [`pass`], [True llama.cpp Vulkan appliance with same-run PASS],
    [`llama-upstream-vk`], [QEMU + Unikraft Vulkan server-only], [`pass`], [Vulkan server appliance reaches model-loaded READY],
  )),
  caption: [Per-app multi-environment evidence matrix. Blocked rows are documented expected states. Native substrate rows use the fake VirtIO-GPU backend. QEMU+Unikraft rows do not promote xport.qemu-vgpu unless the current generated matrix passes it; ring protocol evidence remains native unless same-run QEMU proof exists (proto.venus-ring=PASS natively).]
) <tab:app-multi-env>
